

    <div class="jumbotron">
        <!-- Start panel component -->
        <div class="tn-panel">
            <div class="panel">
                <div class="tn-rte">
                    <h2>{{errorTitle}}</h2>
                    <p>{{errorMessage}}</p>
                </div>
            </div>
        </div>
        <!-- Start panel component -->
    </div>

